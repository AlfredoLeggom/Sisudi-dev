//Hacer efectos al bajar el scroll de la pagina principal

/* $(document).ready(function (){

    $('.gallery__box').hide()

    $(window).scroll(function () {

        let windowHeight = $(window).scrollTop()
        let elementHeight = $('.gallery__box').offset()
        elementHeight = elementHeight.top

        console.log(elementHeight)
        
        if (windowHeight >= elementHeight){
            $('.gallery__box').fadeIn(1000)
        }else{
            $('.gallery__box').hide(1000)  
        }
        
    })
})

*/       